# Secrets & credentials — never commit
.env
*.env
secrets.json
credentials.json
token.json
*_token.json
*_session.json

# macOS
.DS_Store
__pycache__/
*.py[cod]

# Data & sessions (stored locally per user)
sessions/
tmp/

# Python packaging
*.egg-info/
dist/
build/
.venv/
venv/
